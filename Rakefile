# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'

begin
  require 'bundler'
  Bundler.require
rescue LoadError
end

# 例）BubbleWrap の RSS パーサを使う
# 参考：https://github.com/rubymotion/BubbleWrap
#require 'bubble-wrap/rss_parser'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'MySeed'

  # 例）CocoaPods の AFNetworking を使う
  # 参考：https://github.com/HipByte/motion-cocoapods
  #      http://rubymotion.jp/RubyMotionDocumentation/articles/cocoapods/index.html
  #app.pods do
  #  pod 'AFNetworking'
  #end
end
